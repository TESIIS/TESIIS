name: server
description: A sample command-line application.
version: 1.0.0
# This package is not intended to be published to pub.dev (it has local path deps)
publish_to: none
# repository: https://github.com/my_org/my_repo

environment:
  sdk: ^3.9.2

# Add regular dependencies here.
dependencies:
  path: ^1.9.0
  http: ^1.5.0
  shelf: ^1.4.0
  shelf_router: ^1.0.0
  shelf_cors_headers: ^0.1.5
  json_annotation: ^4.9.0
  build_runner: ^2.10.1
  json_serializable: ^6.11.1
  get_it: ^9.0.5
  sqlite3: ^2.9.4
  logging: ^1.2.0

dev_dependencies:
  lints: ^6.0.0
  test: ^1.25.6
